apiVersion: v1
kind: ConfigMap
metadata:
  name: incident-bot-config
  labels:
    app: incident-response-bot
data:
  db-host: "postgres-service"
  db-port: "5432"
  db-name: "incidents"
  db-sslmode: "disable"
  # タイムゾーン設定
  timezone: "Asia/Tokyo"
